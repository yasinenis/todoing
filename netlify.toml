[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback — tüm yollar index.html'e (client routing için)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
