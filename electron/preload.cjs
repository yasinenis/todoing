// Güvenli köprü için ayrılmış preload. Şimdilik native API gerekmiyor;
// ileride bildirim/tepsi (tray) gibi özellikler için IPC buraya eklenebilir.
window.addEventListener("DOMContentLoaded", () => {
  // no-op
});
